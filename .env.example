# Copy to .env (git-ignored). server.js reads this file; Netlify/Vercel use their dashboards instead.
ANTHROPIC_API_KEY=sk-ant-...        # https://console.anthropic.com -> API Keys
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# PORT=3000
# MOCK_AI=1                          # fake judge, no key needed (same as: npm run mock)
